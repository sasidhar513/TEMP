#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


void getTopRight(int size,int i,int j,int &tlx,int &tly)
{
    int minimum =min(i,size-1-j);
    tlx=i-minimum;
    tly=j+minimum;
}
void calculateInitialDistaces(int n,int qx,int qy,int &ld ,int &rd, int &td, int &bd, int &trd,int &tld ,int &bld,int &brd)
{
    //calulating left
    ld=qy;
    rd=n-1-qy;
    td=qx;
    bd=n-1-qx;
    trd=min(qx,n-1-qy);
    tld=min()

}


int main() 
{
    int n,m,qx,qy,x,y;
    cin>>n>>m>>qx>>qy;
    int ld,rd,td,bd,trd,tld,bld,brd;
    calculateInitialDistances(ld,rd,td,bd,trd,tld,bld,brd);

    while(~scanf("%d%d",&x,&y))
    {

    }
    getLeft(n,x,y);

}
